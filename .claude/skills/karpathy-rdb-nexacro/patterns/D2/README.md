# D2 — Detail 2-tier

다행 CRUD 의 기본 nexacro form. 상단 검색 패널, 중앙 grid, 하단 추가/삭제/저장 버튼.

## 요구 endpoints
- select_datalist_map (GET 등) — dsSearch 입력, ds<Entity> 출력
- save_datalist_map (POST 등) — dataList=ds<Entity>:U 입력
